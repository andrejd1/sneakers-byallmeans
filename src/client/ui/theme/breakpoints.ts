export const breakpointSize = {
  mobile: 430,
  tablet: 768,
  laptop: 1024,
  desktop: 1920,
};

export const device = {
  mobile: `(min-width: ${breakpointSize.mobile}px)`,
  tablet: `(min-width: ${breakpointSize.tablet}px)`,
  laptop: `(min-width: ${breakpointSize.laptop}px)`,
  desktop: `(min-width: ${breakpointSize.desktop}px)`,
};
